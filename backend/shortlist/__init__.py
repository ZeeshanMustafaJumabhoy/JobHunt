"""Shortlist: a local job search assistant.

Everything runs on the user's own machine. API keys live in the project's .env,
the resume and profile live in data/, and nothing is sent anywhere except to the
job boards being searched and the AI provider the user picked.
"""

__version__ = "0.1.0"
